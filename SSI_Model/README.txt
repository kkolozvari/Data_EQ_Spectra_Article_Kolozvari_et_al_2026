update 
	analysis_steps.tcl to update time and recorder name: update_duration_for_current_input
	elements.tcl to link GM tags -fx $EQtagsX($index) -fy $EQtagsY($index)
	update definitions.tcl to include -factor [expr 0.5*$SF]